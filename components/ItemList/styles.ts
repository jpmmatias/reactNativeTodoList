import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
  item: {},
  itemText: {
    padding: 16,
    marginTop: 16,
    borderColor: '#bbb',
    borderWidth: 1,
    borderStyle: 'dashed',
    borderRadius: 10,
  },
})
